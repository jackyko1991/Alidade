# Alidade (Flutter app)

This is the Flutter + Rust app itself. See the
[project README](../README.md) for what Alidade does, features, the live
web build, and building/testing instructions — and
[`docs/web-build.md`](../docs/web-build.md) if you're touching the web
(WASM) build specifically.
